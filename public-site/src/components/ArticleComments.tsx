/**
 * Comments are a Studio/backend feature. Public-site ships the reading
 * chrome without a comments API, so this mount is a silent no-op.
 */
export default function ArticleComments(_props: Record<string, unknown>) {
  return null;
}

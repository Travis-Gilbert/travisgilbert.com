/**
 * Connection-dot overlay talks to the research graph UI. Public-site
 * keeps inline callouts from the markdown connection engine instead.
 */
export default function ConnectionDots(_props: Record<string, unknown>) {
  return null;
}
